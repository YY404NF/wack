# wack 网安查课 / 前端页面

本文档基本为手写文档并由我核实，具有很高参考价值，要更改内容时通知我，不要移除这句话。

## 文档约定

1. 本文档只描述前端信息架构、页面职责、交互约束和展示要求，不展开接口参数和数据库字段。
2. 术语统一如下：
   - `页面`：占据主内容区的完整路由视图。
   - `浮窗`：承载较完整表单或列表的模态层。
   - `弹窗`：承载二次确认或轻量提示的模态层。
   - `课次`：某课程组在某一周某一天某一时间节的一次上课。
   - `查课草稿`：用户在当前查课页内尚未提交的临时选择集合，仅保存在当前浏览器本地存储（`localStorage`）中。
3. 所有列表页、日历页和详情页都必须具备 `加载中`、`空数据`、`加载失败` 三种基础状态。
4. 所有按钮、标签、列表项的禁用态都必须明确给出原因，不允许只做灰置不解释。
5. 页面中显示名称时优先展示业务名称，同时保留内部 ID 以便跳转、刷新和回填。

## 面向用户

面向管理员提供横向大屏幕的管理员PC端

面向查课学生/学委提供竖向小屏幕的学生移动端


## 主要布局

### PC端
将整个屏幕视口划分成三块

1. 导航区

左侧竖条，上方有欢迎信息，中间有一级页签按钮，下方有退出登录。

- 主页
- 全院课程表
- 考勤记录
- 考勤日志
- 课程管理
- 班级管理
- 学生管理
- 系统用户管理
- 设置

2. 路径区

右侧顶部横条，进入子页面的时候显示完整路径，可以点击路径上的节点快速回退（类似Win资源管理器路径栏）。

3. 内容区

右侧剩余位置，每个页面的展示位置。

### 移动端
将整个屏幕视口划分成两块

1. 导航区

屏幕底部，具有各个主要页面的页签按钮，更像是浮动在内容区的导航栏。

- 主页
- 查课
- 设置

2. 内容区

屏幕剩余位置，展示页面内容。

### 其他布局

1. 浮窗
叠加在整个屏幕之上，面积较大，内部一般需要查看列表、填写数据。

2. 弹窗
叠加在整个屏幕之上，面积较小，二次确认窗口之类的，用于点击删除或者关闭弹出二次确认删除/放弃弹窗。

3. toast
顶部弹出的不影响点击其他位置的很小的窗口，会自动消失，一般用于通知操作成功或报错。

## 界面 / 页面

### 通用

#### 登录（界面）

- 独立界面，学号 / 工号、密码、登录按钮。

#### 修改密码（浮窗）

- 旧密码、新密码、确认新密码

#### 关于（浮窗）

- 包含整活验证码组件和`关于`浮窗

### PC端

- 导航栏页签切换时保留原页面状态，切回时恢复子级页面深度、（列表）分页和滑动位置，但是内容要刷新，再次点击页签回到一级页面
- 以下`路径`均显示在路径区、页面显示在内容区，子级页面完全覆盖父级页面显示在内容区
- 所有`删除`按钮都需要弹窗二次确认删除。所有具有内容编辑功能的浮窗，如果内容有变动，点击`关闭`按钮时都需要弹窗二次确认放弃。
- 所有筛选条件都需要支持`重置`，列表刷新后保留当前筛选条件。
- 所有列表页默认显示最后一次使用的分页大小；无分页页面不展示伪分页控件。
- 所有通用列表如果不使用分页，就必须使用滑动惰性加载：列表滚动到底部后自动继续加载后续内容，避免一次性展示过多内容。
- 任何具有权限限制或状态限制的操作按钮，都要展示不可操作原因，例如“已有考勤历史，不能删除”。

#### 主页（页面）
`主页`
- 页面左上角具有学期下拉选择框（查询学期表，默认当前学期，无当前学期则显示以前的学期，其他同类学期下拉框口径一致）
- 展示三个排行榜，课程出勤率、班级出勤率、个人出勤率，可以切换正/逆序
- 展示最近完成查课的课次的出勤情况
- 展示最近缺勤、迟到、请假的学生及对应课次

#### 全院课程表（页面）
`全院课程表`
- 页面首行为一组组件：学期下拉选择框、一行周数选择组件（根据当前学期当前周的时间推算，默认查看当前周的课表）、显示周末按钮
- 不再具有筛选条件
- 日历视图。日历首行为周几、首列为时间节。首行默认不显示周六周日
- 日历的第一个格子（第一行第一列）显示切换课程/空闲时间按钮
- 课程日历每个格子内展示课程标签，支持hover查看课程详情
- 空闲时间日历展示空闲时间学生标签

#### 考勤记录（页面）
`考勤记录`
- 独立显示学期下拉选择框、字段不显示在列表中
- 列表形式，显示课次列表，不具有分页，具有筛选条件，每条课次记录有`考勤明细`按钮、`查看修改记录`按钮（跳转到`考勤日志`并自动筛选当前课次）
- 默认不显示没有查课记录的课次。有`显示未查课的课次`按钮，不管显不显示未查课课次，仅显示今天及以前的课次。

##### 考勤明细（页面）
`考勤记录 / 考勤明细`
- 独立显示学期下拉选择框、字段不显示在列表中
- 列表形式，显示该课次所属课程组的学生列表及考勤状态，具有分页和筛选条件，每一条记录有`修改考勤`按钮，还有`查看修改记录`按钮（跳转到`考勤日志`并自动筛选当前课次下的学生）

#### 考勤日志（页面）
`考勤日志`
- 独立显示学期下拉选择框、字段不显示在列表中
- 列表形式，显示考勤日志表的所有记录，具有分页和筛选条件，没有操作按钮。

#### 课程管理（页面）
`课程管理`
- 独立显示学期下拉选择框、字段不显示在列表中。右上角有`创建课程`按钮，弹出创建课程浮窗
- 列表视图，显示课程主表列表，具有分页和筛选条件，有`编辑课程信息`、`编辑课程组信息`和`删除`按钮

##### 课程组管理（页面）
`课程管理 / 课程组管理`
- 右上角有`创建课程组`按钮，弹出创建课程组浮窗
- 列表视图，显示该课程组列表，无需分页和筛选条件，有`编辑课程组信息`、`管理课次`、`编辑上课学生`和`删除`按钮
- 列表左侧额外显示父级课程相关信息

###### 课次管理（页面）
`课程管理 / 课程组管理 / 课次管理`
- 右上角有`创建课次`按钮，弹出创建课次浮窗
- 列表视图，显示该课程组下全部课次，无需分页，支持按周次筛选
- 每条记录有`编辑课次`和`删除`按钮
- 列表左侧额外显示父级课程和课程组相关信息
- 若某课次已有考勤历史，前端仍展示编辑入口，但必须明确提示仅允许修改非破坏性字段或直接禁止并说明原因

###### 上课学生管理（页面）
`课程管理 / 课程组管理 / 上课学生管理`
- 左侧是上课学生列表，无需分页和筛选条件，显示自动聚合成班级、其他学生。
- 班级、班级内的学生、其他学生都有`删除`按钮
- 右侧是班级列表和学生列表，无需分页但是需要惰性加载，需要筛选条件，都有`添加`按钮，添加的学生若不属于课程组的班级的学生，则视为其他学生。
- 列表左侧额外显示父级课程和课程组相关信息

#### 班级管理（页面）
`班级管理`
- 独立显示学期下拉选择框、字段不显示在列表中。右上角有`创建班级`按钮，弹出创建班级浮窗
- 列表形式，显示班级列表，具有分页和筛选条件，每条班级记录有`编辑班级信息`、`编辑班级学生`和`删除`按钮

##### 班级学生管理（页面）
`班级管理 / 班级学生管理`
- 左侧显示父级班级相关信息
- 中间是班级学生列表，无需分页，需要筛选条件，每条有`删除`按钮
- 右侧是所有学生列表，无需分页但是需要惰性加载，需要筛选条件，每条都有`添加`按钮

#### 学生管理（页面）
`学生管理`
- 右上角有`创建学生`按钮，弹出创建学生浮窗
- 列表视图，显示学生列表，具有分页和筛选条件，有`编辑学生信息`和`删除`按钮

#### 系统用户管理（页面）
`系统用户管理`
- 右上角有`创建用户`按钮，弹出创建用户浮窗
- 列表视图，显示系统用户列表，具有分页和筛选条件，有`编辑用户信息`、`更改密码`、`编辑空闲时间`、`管理负责班级`、`冻结`、`解冻`按钮
- 学委行必须展示其负责班级摘要；查课学生和管理员不展示该字段
- 账号被冻结后，若该账号当前在线，则由服务端自动踢下线；前端不提供`手动踢下线`按钮

##### 空闲时间管理（页面）
`系统用户管理 / 空闲时间管理`
- 顶部显示父级用户相关信息、学期下拉选择框
- 日历视图

##### 负责班级管理（页面）
`系统用户管理 / 负责班级管理`
- 顶部显示父级学委相关信息
- 中间显示该学委当前 `managed_class_id` 对应的班级，仅允许存在一条记录，有`清空`按钮
- 右侧显示全部班级列表，支持关键字筛选和惰性加载，每条有`设为负责班级`按钮
- 重新选择负责班级时需要弹窗二次确认覆盖原配置
- 这是用户属性维护页，不是班级维度的反向配置页

#### 设置（页面）
`设置`
- 具有账号设置和学期设置

##### 账号设置
- 管理员自身信息、编辑信息、更改密码

##### 学期设置
- 右上角有`创建学期`，创建学期浮窗中为[数字选择器]（填写当前学期开始年，默认显示当前学年开始年）"-"[自动显示下一年]"-"[下拉框]（1,2）（默认显示当前学期）
- 列表界面，每条有`编辑学期`按钮用于编辑开学时间


### 查课学生端（移动端）

- 查课学生和学委共用此端
- 查课学生的查课页显示今天所有当前可查课程，设置页有编辑空闲时间按钮
- 学委的查课页显示今天自己班级的所有课程，设置页没有编辑空闲时间按钮
- 移动端所有关键提交操作都需要有明确的处理中态，防止重复点击
- 移动端返回列表后需要刷新当前课次状态，避免继续显示已提交前的旧状态

#### 首页

- 顶部欢迎信息
- 下方区域显示当前作息类型以及具体时间。

#### 查课

- 列表界面展示今天范围内的课次
- 查课学生只显示当前时刻可查课次。不显示超过查课时间的课次
- 学委显示所有本班课次，超过查课时间范围的`查课`按钮变更为`已结束`
- 每张课次卡片需要显示课程名、教师名、周次、星期、时间节、地点、所属班级摘要
- 查课规则：
- 课次开课前10分钟，结束后15分钟时间范围内可以查课。例如上午1-2节08:00-08:45,08:55-09:40在`07:50-09:55`范围内可以进入该课次查课界面。
- 查课进行页有`提交查课结果`按钮，已经被提交结果的学生，查课学生/学委再次进入查课进行页时无法再修改该学生考勤状态，只能有管理员能在管理员端的考勤管理页面修改。

#### 查课进行页

- 顶部提供学生姓名搜索框
- 中间为学生列表，展示姓名和当前状态
- 底部提供签到、迟到、缺勤、请假快捷按钮
- 点击快捷按钮后，将当前焦点学生更新为对应状态，并自动切换到下一个学生
- 右上角`返回`按钮，需要弹出二次确认放弃按钮
- 最后点击`提交查课记录`才上传落库

- 每次查课具有`随机查课`模式，每次不一定需要查到所有人，`提交查课记录`按钮不要求所有人都有状态
- 某个课次在可查时间有权限的查课学生/学委都可以进入该课次查课界面
- 进入查课界面时，前端先检查浏览器本地是否存在当前用户、当前课次的草稿；若存在，则弹窗询问是否恢复草稿
- 草稿内容至少包含：所选班级、随机抽查配置、抽中的学生集合、每个学生当前未提交状态
- 草稿恢复时，前端必须先拉取服务端最新学生列表和已提交状态，再执行合并
- 合并规则固定为：服务端已提交状态优先；本地草稿只恢复尚未提交学生的本地状态；已被其他人提交的学生在恢复后显示为锁定且不可修改
- 同一课次允许多人同时查课；页面必须实时体现哪些学生已被他人先提交并锁定
- 提交前需要展示本次已设置人数、未设置人数、随机跳过人数
- 若提交失败，前端草稿必须保留，不能自动清空
- 提交成功后，前端只清理本次已成功提交学生对应的本地草稿状态；仍未提交的本地状态继续保留，直到用户放弃或全部完成

#### 设置

- 显示查课学生自己的相关信息，不能更改信息，可以更改密码
- 查课学生有编辑空闲时间按钮，学委没有
- 下方退出登录按钮
