# wack 网安查课 / 后端接口

本文档基本由AI生成，请AI在编写项目时主动修正本文档，不要移除这句话。

本文档是架构升级阶段的后端接口纸面计划，用于统一后续改造范围、接口分组、权限边界和联调口径。

文档目标：

- 对齐 [README.md](/home/kevin-zhang/projects/wack/wack/README.md) 中的新用户组、学期规则、作息规则和统计目标
- 对齐 [前端页面.md](/home/kevin-zhang/projects/wack/wack/前端页面.md) 中管理员 PC 端与学生移动端页面

本文档不再使用“建议”“可选”“预留”作为主语气。本文档全部内容均视为本轮后端改造的目标基线。

## 1. 文档定位

本文档解决以下问题：

- 后端本期要交付哪些接口
- 每组接口服务哪个页面
- 每个接口允许谁调用
- 哪些接口只要求列出职责，不展开详细参数
- 哪些业务规则属于强约束，后端必须落地

本文档不展开以下内容：

- 最终数据库 DDL 细节
- Redis Key 设计细节
- 代码目录结构与包拆分细节
- OpenAPI 自动生成规范

## 2. 本期改造范围

本期后端改造覆盖以下模块：

- 登录鉴权
- 当前用户信息与修改密码
- 学期、周次、作息时间等全局元数据
- 管理员首页统计
- 全院课程表 / 空闲时间日历
- 考勤记录、考勤明细、考勤日志
- 课程管理、课程组管理、课程组上课学生管理
- 班级管理、班级学生管理
- 学生管理
- 系统用户管理、空闲时间管理
- 学期设置
- 移动端首页
- 移动端查课列表、查课配置、查课提交、查课放弃
- 移动端个人设置

## 3. 角色定义

系统用户角色固定为三类：

- `admin`：管理员
- `checker`：查课学生
- `commissioner`：学委

角色说明：

- 管理员使用 PC 端，拥有全部后台管理权限。
- 查课学生使用移动端，负责学院范围内的日常查课，拥有空闲时间维护能力。
- 学委使用移动端，只能查看和处理自己负责班级的课次，没有空闲时间维护能力。

## 4. 权限总则

## 4.1 接口分区

接口按访问域划分为四类：

- 公共接口：无需登录即可访问
- 通用登录态接口：任意已登录用户可访问
- 管理端接口：仅 `admin` 可访问
- 移动端接口：`checker` 与 `commissioner` 可访问，但数据范围不同

## 4.2 数据范围约束

管理员：

- 可访问全部数据
- 可修改全部考勤结果
- 可管理全部课程、课程组、班级、学生、系统用户、学期

查课学生：

- 可查看当前时刻处于可查窗口内的课次
- 可进入有权限的课次查课流程
- 可提交自己本次选中的学生考勤结果
- 不可修改其他人已经落库的学生考勤状态
- 可维护自己的空闲时间

学委：

- 可查看自己负责班级当天的课次
- 可在允许窗口内提交自己负责班级学生的考勤结果
- 不可查看无关班级学生
- 不可维护空闲时间
- 不可访问管理端接口

## 4.3 强制权限模型

本期后端固定使用用户表中的 `managed_class_id` 表达学委的数据范围。

关系含义：

- 一个学委只能负责一个班级
- 班级表不反向记录学委
- 学委的课次列表、查课对象、提交范围均以其 `managed_class_id` 为准

## 5. 通用接口规范

## 5.1 基础前缀

- 基础路径前缀统一为 `/api`

## 5.2 认证方式

- 登录成功后返回 JWT
- 登录后请求统一通过 `Authorization: Bearer <token>` 传递登录态

## 5.3 统一返回格式

成功：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

失败：

```json
{
  "code": 40001,
  "message": "参数错误",
  "data": null
}
```

## 5.4 列表返回格式

分页列表：

```json
{
  "items": [],
  "page": 1,
  "page_size": 20,
  "total": 0
}
```

非分页列表：

```json
[]
```

说明：

- 非分页接口当前以“直接返回数组”为主；少量接口返回具名对象时，会在具体接口章节单独说明

## 5.5 时间与枚举

- 时间统一使用 ISO 8601
- 星期：`1-7` 表示周一到周日
- 时间节：`1-5` 表示五个固定时间片
- 考勤状态：
  - `0` 签到
  - `1` 迟到
  - `2` 缺勤
  - `3` 请假
- 系统用户状态：
  - `1` 正常
  - `2` 冻结

## 5.6 错误码

- `40001` 参数错误
- `40101` 未登录或 token 无效
- `40301` 无权限
- `40401` 资源不存在
- `40901` 数据冲突
- `40902` 不在允许查课时间范围内

## 6. 业务强约束

以下规则属于后端必须保证的业务约束：

- 同一 `course_group_lesson` 下，同一学生只允许存在一条最终 `attendance_record`
- 只为本次实际被查到且被设置结果的学生创建 `attendance_record`；未被查到学生不创建记录
- 移动端学生操作先在浏览器本地草稿中完成，点击提交后才落库
- 同一课次允许多人同时查课；某学生一旦已有 `attendance_record`，后续来自其他查课学生和学委的提交不得覆盖，首次成功提交为准
- 管理员可以修改任意学生已有考勤记录的状态；如管理员需要补录未被查学生，则创建新的 `attendance_record`
- 查课可进入时间固定为“开课前 10 分钟到结束后 15 分钟”
- 学委只能处理自己负责班级的学生
- 学委在移动端看到的课次列表包含本班今日课次，超出可查窗口时按钮状态为“已结束”
- 查课页面点击返回时，表示放弃本次未提交草稿
- 放弃草稿只影响当前前端临时状态，不产生后端删除动作
- 所有考勤状态变更都必须写入 `attendance_record_log`
- 账号被冻结后，若当前存在有效会话，服务端必须立即删除该账号全部会话，使其自动下线

## 7. 接口清单写法说明

下文每个接口组统一按以下字段描述：

- 页面：对应前端页面或场景
- 权限：允许访问的角色
- 范围：该角色能看到或修改的数据边界
- 接口：本期需要实现的接口列表

简单接口不展开完整参数和返回体，只保留职责和权限边界。核心链路接口保留较明确的请求语义。

## 8. 公共与通用登录态接口

## 8.0 初始化接口

页面：

- 首次部署初始化页

权限：

- 公共接口

接口：

- `GET /api/setup/status`
- `POST /api/setup/initialize`

说明：

- 用于判断系统是否已初始化，以及完成首次管理员账号初始化

## 8.1 认证接口

页面：

- 登录页
- 全端登录态恢复

权限：

- 公共接口：`/auth/login`
- 登录态接口：`/auth/me`、`/auth/logout`、`/auth/change-password`

范围：

- `login` 面向全部系统用户
- `me` 返回当前登录用户自身信息
- `change-password` 只允许修改本人密码

接口：

- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/me`
- `POST /api/auth/change-password`
- `PUT /api/auth/profile`

说明：

- 登录返回用户基础信息、角色、状态、权限范围信息
- 学委登录时必须返回其 `managed_class_id` 及对应班级摘要
- `logout` 设计为幂等接口；token 已失效或 Redis 会话已不存在时仍直接返回成功

## 8.2 全局元数据接口

页面：

- 管理员首页
- 全院课程表
- 学期下拉框
- 移动端首页
- 移动端查课页

权限：

- 任意已登录用户

范围：

- 所有人都可获取当前学期、周次、作息时间等公共元数据

接口：

- `GET /api/meta/context`
- `GET /api/meta/terms`
- `GET /api/meta/terms/{term_id}/weeks`
- `GET /api/meta/sections`

## 8.3 通用空闲时间与系统设置接口

页面：

- 管理员 `系统用户管理 / 空闲时间管理`
- 查课学生移动端 `设置 / 空闲时间`
- 移动端首页、查课页

权限：

- `free-times`：任意已登录用户
- `system-settings`：任意已登录用户可读；管理员另有管理端写接口

范围：

- `checker` 只能维护自己的空闲时间
- `admin` 可通过 `login_id` 查询和维护指定查课学生空闲时间
- `commissioner` 不允许创建、修改、删除空闲时间

接口：

- `GET /api/free-time-editor`
- `GET /api/free-times`
- `POST /api/free-times`
- `PUT /api/free-times/{id}`
- `DELETE /api/free-times/{id}`
- `GET /api/system-settings`

说明：

- `free-time-editor` 为弹窗/编辑器专用轻量接口，只返回当前编辑所需字段，不返回列表页和日历页不需要的用户字段
- `free-times` 保留分页明细查询能力，供列表页和管理页使用

## 9. 管理端接口

管理端所有接口统一要求：

- 权限：仅 `admin`
- 认证失败返回 `40101`
- 非管理员访问返回 `40301`

## 9.1 管理员首页

页面：

- `主页`

权限：

- `admin`

范围：

- 可查看所选学期全院统计

接口：

- `GET /api/admin/overview`

职责：

- 返回课程出勤率排行榜
- 返回班级出勤率排行榜
- 返回个人出勤率排行榜
- 返回最近完成查课课次
- 返回最近缺勤、迟到、请假学生

## 9.2 全院课程表 / 空闲时间日历

页面：

- `全院课程表`

权限：

- `admin`

范围：

- 可查看所选学期、所选周次的全院课程日历与空闲时间日历

接口：

- `GET /api/admin/course-calendar`
- `GET /api/admin/free-time-calendar`

职责：

- 返回周课表格子数据
- 返回课程格子详情
- 返回空闲时间格子详情

## 9.3 考勤记录

页面：

- `考勤记录`
- `考勤记录 / 考勤明细`

权限：

- `admin`

范围：

- 可查看全部课次的考勤记录与明细
- 可修改任意学生已有考勤记录的状态

接口：

- `GET /api/admin/attendance-results`
- `GET /api/admin/attendance-sessions/{course_group_lesson_id}`
- `PATCH /api/admin/attendance-sessions/{course_group_lesson_id}/students/{student_id}/status`
- `PATCH /api/admin/attendance-records/{attendance_record_id}/status`
- `GET /api/admin/attendance-records/{attendance_record_id}/logs`

要求：

- 列表接口支持学期筛选
- 课次列表默认只显示今天及以前的课次
- 课次列表默认不显示无查课记录课次
- 修改考勤时必须写 `attendance_record_log`
- 课次下某学生若不存在 `attendance_record`，表示该学生本次未被查到，而不是存在一条“无记录”状态
- 管理员在课次明细中可以对“未被查到”的学生直接补录状态；此时后端创建新的 `attendance_record`

## 9.4 考勤日志

页面：

- `考勤日志`

权限：

- `admin`

范围：

- 可查看全部考勤状态修改记录

接口：

- `GET /api/admin/attendance-record-logs`

要求：

- 支持按课次、学生、操作人、状态变化、时间范围筛选

## 9.5 课程管理

页面：

- `课程管理`

权限：

- `admin`

范围：

- 可管理课程主数据

接口：

- `GET /api/admin/courses`
- `POST /api/admin/courses`
- `PUT /api/admin/courses/{course_id}`
- `DELETE /api/admin/courses/{course_id}`

要求：

- 课程列表支持学期筛选
- 删除课程时必须校验是否仍存在课程组、课次、考勤历史

## 9.6 课程组管理

页面：

- `课程管理 / 课程组管理`

权限：

- `admin`

范围：

- 可管理某门课程下的全部课程组

接口：

- `GET /api/admin/courses/{course_id}/groups`
- `POST /api/admin/courses/{course_id}/groups`
- `GET /api/admin/courses/{course_id}/groups/{group_id}`
- `DELETE /api/admin/courses/{course_id}/groups/{group_id}`
- `GET /api/admin/courses/{course_id}/groups/{group_id}/sessions`

要求：

- 课程组是一等业务实体，不再只用旧版课程字段隐式承载
- 课程组负责承载教学班 / 合班 / 固定上课安排
- 修改课程组排课信息时必须校验历史考勤影响

## 9.7 课程组上课学生管理

页面：

- `课程管理 / 课程组管理 / 上课学生管理`

权限：

- `admin`

范围：

- 可查看、添加、移除课程组的班级与学生

接口：

- `GET /api/admin/courses/{course_id}/groups/{group_id}/students`
- `GET /api/admin/courses/{course_id}/groups/{group_id}/available-classes`
- `POST /api/admin/courses/{course_id}/groups/{group_id}/classes`
- `DELETE /api/admin/courses/{course_id}/groups/{group_id}/classes/{class_id}`
- `GET /api/admin/courses/{course_id}/groups/{group_id}/available-students`
- `POST /api/admin/courses/{course_id}/groups/{group_id}/students`
- `DELETE /api/admin/courses/{course_id}/groups/{group_id}/students/{student_id}`

要求：

- 已选学生按“班级”“其他学生”分组返回
- 候选班级、候选学生列表支持关键字搜索与惰性加载
- `available-classes`、`available-students` 两条候选接口支持 `page`、`page_size`、`keyword`
- 删除课程组班级或学生时必须校验是否已有历史考勤

## 9.8 班级管理

页面：

- `班级管理`

权限：

- `admin`

范围：

- 可管理全部班级主数据

接口：

- `GET /api/admin/classes`
- `POST /api/admin/classes`
- `GET /api/admin/classes/{class_id}`
- `PUT /api/admin/classes/{class_id}`
- `DELETE /api/admin/classes/{class_id}`

要求：

- 删除班级时不得删除学生实体；仅清空学生所属班级、课程组学生中的班级身份以及学委负责班级等关联字段

## 9.9 班级学生管理

页面：

- `班级管理 / 班级学生管理`

权限：

- `admin`

范围：

- 可查看、添加、导入、移除班级学生

接口：

- `GET /api/admin/classes/{class_id}/students`
- `POST /api/admin/classes/{class_id}/students/import`
- `POST /api/admin/classes/{class_id}/students`
- `PUT /api/admin/classes/{class_id}/students/{student_id}`
- `DELETE /api/admin/classes/{class_id}/students/{student_id}`
- `GET /api/admin/class-students`

要求：

- 班级学生列表支持关键字筛选
- 候选学生列表支持关键字搜索与惰性加载
- 支持通过 `csv` 文件导入班级学生
- 导入接口使用 `multipart/form-data`，文件字段名为 `file`
- 示例模板首行前两列固定为 `学号,姓名`
- 导入时允许按 `学号` 覆盖更新已有学生的姓名与所属班级
- 空行允许跳过；同一文件内学号重复或表头不合法时返回请求错误

## 9.10 学生管理

页面：

- `学生管理`

权限：

- `admin`

范围：

- 可管理课程域学生主数据

接口：

- `GET /api/admin/students`
- `POST /api/admin/students`
- `PUT /api/admin/students/{student_id}`
- `DELETE /api/admin/students/{student_id}`

要求：

- 这里的学生不是系统登录用户
- 删除学生时必须校验其班级、课程组、考勤历史引用
- 创建学生时若 `student_no` 已存在，必须直接返回冲突错误，不允许按学号覆盖更新原记录

## 9.11 系统用户管理

页面：

- `系统用户管理`
- `系统用户管理 / 空闲时间管理`
- `系统用户管理 / 负责班级管理`

权限：

- `admin`

范围：

- 可管理管理员、查课学生、学委三类系统用户
- 可配置学委负责班级
- 可查看和编辑查课学生空闲时间

接口：

- `GET /api/admin/users`
- `POST /api/admin/users`
- `GET /api/admin/users/{login_id}`
- `PUT /api/admin/users/{login_id}`
- `PATCH /api/admin/users/{login_id}/password`
- `PATCH /api/admin/users/{login_id}/status`
- `PATCH /api/admin/users/{login_id}/role`
- `GET /api/free-time-editor`
- `GET /api/free-times`
- `POST /api/free-times`
- `PUT /api/free-times/{id}`
- `DELETE /api/free-times/{id}`

要求：

- 创建和编辑学委时，必须维护其 `managed_class_id`
- 管理员不能冻结自己
- 管理员不能取消自己的管理员身份
- 冻结用户时，若该用户当前在线，服务端必须立即清除其全部会话
- 学委没有空闲时间管理能力，因此管理员不为学委配置空闲时间
- 负责班级维护当前通过 `managed_class_id` 融合在用户创建/编辑接口中完成，尚未拆成独立专用后端接口

## 9.12 系统设置 / 学期设置

页面：

- `设置`
- `设置 / 学期设置`

权限：

- `admin`

范围：

- 个人资料接口只作用于当前管理员本人
- 学期设置作用于全局学期配置

接口：

- `PUT /api/auth/profile`
- `GET /api/admin/system-settings`
- `PUT /api/admin/system-settings`
- `GET /api/meta/terms`
- `POST /api/admin/terms`
- `PUT /api/admin/terms/{term_id}`

要求：

- 学期列表服务所有学期下拉源头
- 学期编辑至少支持开学时间维护
- 学期主数据读取统一走 `/api/meta/terms`，管理员只负责新增和更新

## 10. 移动端接口

移动端接口统一要求：

- 权限：`checker`、`commissioner`
- 学委的数据范围受其 `managed_class_id` 约束

## 10.1 移动端首页

页面：

- `主页`

权限：

- `checker`
- `commissioner`

范围：

- 返回当前登录用户自己的欢迎信息与全局作息信息

接口：

- `GET /api/meta/context`
- `GET /api/meta/sections`

## 10.2 查课列表

页面：

- `查课`

权限：

- `checker`
- `commissioner`

范围：

- `checker`：可查看当前时刻处于允许查课窗口内的课次
- `commissioner`：可查看自己负责班级当天全部课次

接口：

- `GET /api/student/courses/available`

要求：

- 学委超出查课窗口的课次必须返回“已结束”状态
- 查课学生不返回已经超出查课窗口的课次

## 10.3 进入或继续查课

页面：

- 点击课次卡片后的查课流程

权限：

- `checker`
- `commissioner`

范围：

- `checker`：可进入有权限的课次，并获取当前课次下全部学生与已有记录
- `commissioner`：只能进入并查看自己负责班级范围内的学生

接口：

- `POST /api/student/attendance-sessions`
- `GET /api/student/attendance-sessions/{course_group_lesson_id}`

要求：

- 查课草稿仅存在于浏览器本地存储，不创建独立持久化记录
- `POST /api/student/attendance-sessions` 只负责进入课次并完成权限与时间窗口校验
- `GET /api/student/attendance-sessions/{course_group_lesson_id}` 返回课次、课程、班级分组和当前学生列表
- 服务端返回中已存在 `attendance_record` 的学生视为已锁定；前端本地草稿只能恢复尚未提交的学生状态

## 10.5 提交查课结果

页面：

- `查课进行页`

权限：

- `checker`
- `commissioner`

范围：

- `checker`：可提交自己本次有权限范围内的学生状态
- `commissioner`：只能提交自己负责班级学生状态

接口：

- `POST /api/student/attendance-sessions/{course_group_lesson_id}/submit`

核心请求语义：

```json
{
  "items": [
    {
      "student_ref_id": 1,
      "status": 0
    }
  ]
}
```

要求：

- 仅提交本次已设置状态的学生
- 后端逐条校验权限与时间窗口
- 对于尚未存在最终考勤结果的学生，按请求状态写入最终记录
- 对于已存在最终考勤结果的学生，后端直接忽略本次提交项，不报错、不覆盖
- 每次状态落库都必须写 `attendance_record_log`
- 响应体必须返回本次 `accepted_items` 与 `ignored_items`，用于前端清理或保留本地草稿
- `student_ref_id` 当前固定传学生 ID，用于表达“本次提交针对哪位学生”

## 10.6 查课进行中的单条调整、完成与放弃

页面：

- `查课进行页` 返回确认弹窗

权限：

- `checker`
- `commissioner`

范围：

- 只允许调整或结束当前用户正在处理的本次查课流程

接口：

- `PATCH /api/student/attendance-records/{attendance_record_id}/status`
- `POST /api/student/attendance-sessions/{course_group_lesson_id}/complete`
- `DELETE /api/student/attendance-sessions/{course_group_lesson_id}`

要求：

- `PATCH` 只允许修改当前用户本次已有且未被他人锁定的记录
- `complete` 用于前端显式结束本次查课流程
- `DELETE` 表示放弃当前浏览器本地草稿，不回滚已提交数据
- 后端不单独维护草稿实体

## 10.7 移动端设置

页面：

- `设置`

权限：

- `checker`
- `commissioner`

范围：

- 两者都只能查看自己的设置页数据
- 只有 `checker` 可以维护自己的空闲时间

接口：

- `GET /api/auth/me`
- `POST /api/auth/change-password`
- `GET /api/student/managed-class`
- `GET /api/free-time-editor`
- `GET /api/free-times`
- `POST /api/free-times`
- `PUT /api/free-times/{id}`
- `DELETE /api/free-times/{id}`

要求：

- `commissioner` 调用空闲时间写接口时返回 `40301`
- 学委设置页负责班级信息与班级学生列表统一走 `/api/student/managed-class`

## 11. 接口与页面映射

管理员 PC 端：

- `主页` 对应 `admin/attendance-dashboard`
- `全院课程表` 对应 `admin/course-calendar`、`admin/free-time-calendar`
- `考勤记录` 对应 `admin/attendance-results`、`admin/attendance-sessions/{id}`
- `考勤日志` 对应 `admin/attendance-record-logs`
- `课程管理` 对应 `admin/courses*`
- `课程组管理` 对应 `admin/courses/{course_id}/groups*`
- `上课学生管理` 对应 `admin/courses/{course_id}/groups/{group_id}/*`
- `班级管理` 对应 `admin/classes*`
- `班级学生管理` 对应 `admin/classes/{class_id}/students*`
- `学生管理` 对应 `admin/students*`
- `系统用户管理` 对应 `admin/users*`
- `空闲时间管理` 对应 `free-times*`
- `系统设置` 对应 `auth/profile`、`admin/system-settings`
- `学期设置` 对应 `admin/terms*`

移动端：

- `主页` 对应 `meta/context`、`meta/sections`
- `查课` 对应 `student/courses/available`
- `查课进行页` 对应 `student/attendance-sessions*`
- `提交查课记录` 对应 `student/attendance-sessions/{course_group_lesson_id}/submit`
- `放弃查课` 对应 `student/attendance-sessions/{course_group_lesson_id}`
- `设置` 对应 `auth/me`、`auth/change-password`、`student/managed-class`、`free-times*`

## 12. 改造落地顺序

按联调优先级，本期后端改造顺序固定为：

1. 认证接口与全局元数据接口
2. 移动端查课主链路
3. 管理端考勤记录与考勤日志
4. 课程、课程组、课程组上课学生管理
5. 班级、学生、系统用户管理
6. 管理员首页、全院课程表、学期设置

## 13. 本文档相对旧稿的收敛点

本稿相对上一版后端接口文档，做了以下收敛：

- 从“接口建议稿”改为“本期纸面计划”
- 为每组接口补齐了权限与数据范围
- 把前后端联调重点放在接口职责，而不是无差别展开参数
- 明确学委必须依赖用户表中的 `managed_class_id` 进行权限裁剪
- 将课程组确定为本期必须落地的一等业务实体
- 把强业务约束单独收敛为统一规则，避免散落在接口说明中
