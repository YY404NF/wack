# wack 网安查课 / 改造进度

本文档基本由AI生成，请AI在改造项目时主动向本文档同步改造进度，不要移除这句话。

## 1. 目标

- 以 `wack/` 主文档为唯一正式基线，继续删除旧术语、旧结构、旧页面流和阶段性兼容层。
- 前端按 [前端页面.md](/home/kevin-zhang/projects/wack/wack/前端页面.md) 完成管理员 PC 端与学生移动端页面落地。
- 后端按 [后端接口.md](/home/kevin-zhang/projects/wack/wack/后端接口.md) 与 [数据结构.md](/home/kevin-zhang/projects/wack/wack/数据结构.md) 完成接口、查询、权限和数据库结构收口。
- Redis 不只承担会话，还要继续落地缓存、幂等、短锁、限流等能力。

## 2. 当前判断

- 总进度：`88%`
- 核实口径：
  - 主文档已经基本成型，后端主表结构和主要业务口径已大幅收口。
  - 2026-03-19 已补做一轮文档核查，`后端接口.md` 已从旧 `/api/v1` 纸面口径收口到当前 `/api`，并补进近期新增接口。
  - 前端“能用”部分已经明显增加，但距离文档要求的完整页面层、子页面深度、交互一致性还有缺口。
  - 2026-03-19 已完成一次较底层的考勤语义收口：不再用“无记录状态值”，改为“存在 `attendance_record` 才表示被查到”。
  - 因此整体进度不能按“底层结构已改”高估，仍以页面层和剩余旧口径清理为主。

## 3. 已完成

- 文档基线已形成：`README.md`、[前端页面.md](/home/kevin-zhang/projects/wack/wack/前端页面.md)、[后端接口.md](/home/kevin-zhang/projects/wack/wack/后端接口.md)、[数据结构.md](/home/kevin-zhang/projects/wack/wack/数据结构.md)、`Redis设计.md`
- 文档与实现已继续对齐：`后端接口.md` 已补齐初始化、认证资料、共享空闲时间、学委负责班级、管理员考勤补录、学期设置等当前真实接口，并统一使用 `/api`
- 工程基线已落地：前端 Axios、Pinia、VueUse、环境示例；后端 Redis 会话基础能力
- 已删除旧设计：系统日志、课程/班级导入、旧考勤路径、旧课程替换型接口
- 课程域主链路已切到课程组模型：课程组、课次、课程组学生接口与管理页面已可用
- 学生端查课主链路已可用：草稿、并发首写生效、提交/放弃、`course_group_lesson` 主口径已落地
- 考勤域已明显收口：`attendance_record / attendance_record_log` 已成为主结构，旧 `attendance_session` 已从本地数据库与建表逻辑中删除
- 考勤状态枚举已完成主代码收口：前后端统一改为 `0=签到，1=迟到，2=缺勤，3=请假`
- “无记录”语义已从状态值中移除：移动端查课不再预创建考勤记录，是否被查到改为通过 `attendance_record` 是否存在判断
- 管理员考勤明细已切到新语义：课次明细返回全部学生，未被查到学生显示为“未查”，管理员可直接补录状态
- 考勤日志已补“create_record / set_status”区分，首次创建记录不再伪装成“未设置 -> 某状态”
- 通用列表已开始收口到真实数据加载语义：
  - 具有分页组件的管理员主列表已切到真实后端分页，不再用前端整表切片伪分页
  - 不具有分页组件的学期设置列表已改为滚动到底继续加载后续内容
  - 管理员“考勤记录”页已补做课次汇总分页与课次明细分页
- 全院课程表默认周数已改为按所选学期的 `term_start_date` 自动计算，不再依赖 `system_setting.current_term_start_date`
- 已开始为弹窗与选择器拆轻量接口：
  - 班级选项改走 `class-options`
  - 班级学生编辑弹窗右侧未绑定学生改走 `student-options?binding=unbound`
- 已继续压缩“全量查询”：
  - 空闲时间编辑器已补专门轻量接口 `free-time-editor`，管理员代改和学生本人编辑不再拉整份空闲时间列表
  - 全院课程表已移除无意义的全量班级拉取
  - 前端 API 中未再被主链路使用的 `listAllClasses / listAllStudents / listAllUsers / listAllCourses / listAllFreeTimes` helper 已删除，避免回退到全量取数
- 首屏组件加载已开始拆分：
  - `App.vue` 已改为异步加载 `SetupPage / LoginPage / AdminWorkspace / StudentWorkspace`
  - 管理员内容区各页签面板已改为异步组件，避免首次进入管理员端就把所有管理页面板一起加载
  - 移动端内容区已继续拆成异步 `首页 / 查课 / 设置` 子面板，避免首次进入移动端就把三个 tab 的组件和查课重逻辑一起加载
- 数据结构已继续校正：`user` 表字段顺序已调整，`login_id` 口径已开始替代旧 `student_id`
- 学生管理已从占位页变成真实页面：后端独立学生 CRUD 已接通，前端已有列表、筛选、分页、创建、编辑、删除
- 空闲时间历史数据已迁移：备份库 `wack.db.bak-20260319-091554` 已迁入新库 `user_free_time`，统一归属到学期 `2025-2026-2`
- 管理员首页和全院课程表已做过一轮页面改造，但仍需继续向文档细节收口

## 4. 前端后续方向

- 大量异步升级任务：
  - 继续把管理员端重弹窗改成异步组件：
    - 课程管理相关弹窗
    - 班级学生编辑弹窗
    - 用户管理更改密码/编辑空闲时间弹窗
    - 学期设置创建/编辑弹窗
  - 继续把管理员端复杂子区域拆成异步子组件：
    - 课程管理中的课程组工作区
    - 考勤记录中的课次详情区与记录日志区
    - 班级管理中的双列表学生编辑区
  - 继续把移动端非当前 tab 的重逻辑从首屏剥离：
    - 查课全屏工作区继续评估是否拆成独立异步模块
    - 设置页中的空闲时间编辑器、班级学生弹窗、密码弹窗继续评估是否延后加载
  - 对所有异步页面补统一体验：
    - `Suspense` 或同级加载占位
    - 切换中的骨架/加载态
    - 异步失败时的局部重试
  - 继续检查“组件异步了，但数据仍一次性全拉”的问题，避免只做包拆分不做数据拆分
- 补齐管理员页面层：
  - `负责班级管理`
  - `学期设置`
  - `空闲时间管理`
  - `班级学生管理`
- 继续深化已有页面：
  - `考勤记录` 需要继续核对“课次列表 -> 考勤明细 -> 跳转考勤日志”的联动、筛选与状态刷新细节
  - `考勤日志` 需要继续核对筛选、跳转联动和字段展示
  - `课程管理` 需要继续按文档拆实“课程组管理 / 课次管理 / 上课学生管理”子页面深度
  - `系统用户管理` 需要继续补齐更改密码、编辑空闲时间、管理负责班级等完整操作流
  - `系统设置` 需要继续补齐管理员自维护和学期入口
- 统一页面交互规范：
  - 所有列表页、详情页、日历页都要持续核对 `加载中 / 空数据 / 加载失败`
  - 所有分页、筛选、顶部操作区、禁用原因提示继续统一到同一套风格
  - 路径区、子页面回退、页签切换保留状态仍需继续细抠
- 继续核查非通用列表的大查询必要性：
  - 全院课程表当前仍是按页面需要加载所选学期下的课程与空闲时间聚合，不是分页列表；后续继续评估是否还要按周数、楼宇或可视区块继续拆分
  - 首页统计、排行榜、最近记录等摘要页继续优先走聚合接口和缓存，不回退到明细全量拉取
- 移动端继续核对：
  - 首页、查课页、设置页还要继续按文档检查按钮状态、时间展示、权限差异和提交态

## 5. 后端后续方向

- 大量异步升级配套任务：
  - 给前端异步页面继续补按需接口，避免“组件延后加载，但接口仍然首屏一起打”
  - 将重聚合接口继续拆成首屏摘要接口与进入子页面后的详情接口
  - 为弹窗和选择器继续补 option / editor / summary 专用接口，避免异步弹窗打开时还要请求整页数据
  - 为首页、全院课程表、考勤统计等高频异步页面准备缓存层，降低切换页签和回访时的首包等待
  - 为后续更多并发异步请求补幂等、短锁和缓存失效策略，避免局部异步升级后服务端热点抖动
- 继续清理旧口径：
  - 删除剩余过渡字段名、查询别名、兼容响应键
  - 将用户域剩余旧 `student_id` 账号命名彻底收口到 `login_id`
- 继续补齐文档接口：
  - `负责班级管理`
  - `学期设置`
  - 前端页面层新增/深化部分所需的配套接口和筛选能力
- 继续补轻量接口替代“全量字段/全量记录”：
  - 弹窗、选择器、编辑器优先走 option / editor / summary 专用接口
  - 继续排查仍有必要的聚合查询，避免把重页面错误做成“全量记录 + 前端过滤”
- 开始清理 `system_setting` 旧设计：
  - 从后端路由、服务、查询、前端调用和文档摘要层彻底移除 `system_setting` 相关内容
  - 将当前作息和学期开学日期读取能力收口到 `term` 与作息规则本身，不再保留额外系统设置表依赖
- 继续核对课程域与考勤域：
  - 查询字段、统计聚合、删除约束、逻辑删除语义继续对齐 [数据结构.md](/home/kevin-zhang/projects/wack/wack/数据结构.md)
  - 避免“表名新了、返回字段还是旧口径”的过渡状态长期存在
  - 继续核对考勤日志“首次创建记录”的最终展示与审计语义，确认是否需要继续细化
- 继续落地 Redis 非会话能力：
  - 元数据缓存
  - 首页统计缓存
  - 提交幂等
  - 短锁
  - 限流

## 6. 当前优先级

- 第一优先级：补齐前端页面层，尤其是文档里已经明确存在但仍未完整落地的管理页与子页面
- 第二优先级：继续做前后端异步升级，既拆组件首包，也拆数据首包
- 第三优先级：删除剩余兼容字段和旧术语，继续压缩过渡层
- 第四优先级：补 Redis 非会话能力，完善工程化

## 7. 风险

- 不能把“数据库主表已大致收口”误判为“整体改造已接近完成”；前端页面层仍是当前最大缺口
- 当前仍存在“外层页面已经改新、内部字段或接口别名还没删净”的过渡状态，后续必须继续删
- 当前选择的是直接改造，不做迁移脚本，因此运行环境必须保证数据库结构与当前代码一致
- 异步升级如果只拆组件、不拆数据查询，容易出现“包小了但接口仍然首屏阻塞”的假优化
- 异步升级如果没有统一加载态和错误态，页面会出现闪烁、空白或局部交互断裂

## 8. 约束

- 不修改 `wack-backend/README.md`、`wack-frontend/README.md`、`README_old.md`
- 兼容性代码允许阶段性存在，但最终必须删除
- `wack/` 主文档中已移除的设计最终都必须从代码里删除
