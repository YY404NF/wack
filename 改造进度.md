# wack 网安查课 / 改造进度

本文档基本由AI生成，请AI在改造项目时主动向本文档同步改造进度，不要移除这句话。

## 1. 目标

- 以 `wack/` 目录文档为唯一正式基线，持续删除旧术语、旧结构、旧页面流和阶段性兼容层。
- 前端按 [前端页面.md](/home/kevin-zhang/projects/wack/wack/前端页面.md) 完成管理员 PC 端与学生移动端页面落地。
- 后端按 [后端接口.md](/home/kevin-zhang/projects/wack/wack/后端接口.md) 与 [数据结构.md](/home/kevin-zhang/projects/wack/wack/数据结构.md) 完成接口、权限、查询和删除语义收口。
- Redis 后续继续承担缓存、幂等、短锁、限流等非会话能力。

## 2. 当前判断

- 总进度：`89%`
- 当前阶段已经不是“主结构设计”问题，主要是页面细节、删除语义、轻量接口和残余旧口径清理。
- 课程域和考勤域主链路已经切到课程组、课次、课程组学生、`attendance_record / attendance_record_log` 新模型。
- 最大剩余工作集中在管理员页面层收尾、交互一致性、以及少量后端配套接口和删除规则补完。

## 3. 已完成

### 文档与基线

- `README.md`、[前端页面.md](/home/kevin-zhang/projects/wack/wack/前端页面.md)、[后端接口.md](/home/kevin-zhang/projects/wack/wack/后端接口.md)、[数据结构.md](/home/kevin-zhang/projects/wack/wack/数据结构.md)、`Redis设计.md` 已形成当前基线。
- `后端接口.md` 已统一到当前 `/api` 路径，不再沿用旧 `/api/v1` 纸面口径。

### 后端主链路

- 登录鉴权、用户资料、修改密码、基础元数据、课程域、考勤域、学生/班级/用户管理主接口已接通。
- 课程组、课次、课程组上课学生三层接口已落地。
- 考勤主口径已收口到 `attendance_record / attendance_record_log`，不再使用“无记录状态值”。
- 管理员考勤明细已支持“已被查到”和“未查到”新语义。
- 课程组班级/学生删除已按文档补到“有历史考勤则逻辑删除，无历史考勤才物理删除”。
- 课程组班级/学生添加已改为事务内手动 upsert，不再依赖高风险数据库冲突写法。

### 前端主链路

- 管理员端主页面已覆盖：主页、全院课程表、考勤记录、考勤日志、课程管理、班级管理、学生管理、系统用户管理、设置。
- 学生端主页面已覆盖：首页、查课、设置。
- 课程管理已补出三层子页面流：
  - `课程管理 / 课程组管理`
  - `课程管理 / 课程组管理 / 课次管理`
  - `课程管理 / 课程组管理 / 上课学生管理`
- 课程组上课学生管理已接入真实候选班级、候选学生接口，并使用惰性加载。
- 课程组上课学生管理已补统一确认弹窗，不再使用浏览器原生确认框。
- 课程管理子页面深链接、刷新恢复、路径栏回退已做过一轮修复。

### 通用能力

- 通用列表组件已支持真实分页、惰性加载、汇总栏、跳页输入。
- 管理员主要面板和学生端主要内容区已开始异步加载，首屏包体已做过一轮压缩。
- 多处全量拉取已替换为 option / editor / summary / page 型轻量接口。

## 4. 未完成

### 前端

- 课程管理仍需继续打磨：
  - 课程组信息编辑动作尚未最终收口。
  - 上课学生管理的布局、列表密度、操作提示仍在收尾。
  - 子页面高度、滚动区、状态保留仍需继续细抠。
- 系统用户管理仍需继续完善：
  - 更改密码
  - 空闲时间管理
  - 负责班级管理
- 设置页仍需继续收口到文档中的“账号设置 + 学期设置”完整形态。
- 统一交互规范仍未完全完成：
  - `加载中 / 空数据 / 加载失败`
  - 禁用原因提示
  - 列表筛选与重置
  - 路径栏与页签切换状态

### 后端

- 仍需继续补轻量接口，避免复杂页面或弹窗回退到整页重拉。
- 仍需继续清理残余旧命名、兼容字段和过渡层。
- `system_setting` 旧设计仍未完全退出，后续要继续向 `term` 与作息规则收口。
- Redis 的缓存、幂等、短锁、限流仍主要停留在规划阶段。

## 5. 当前优先级

1. 继续收尾课程管理相关页面，尤其是课程组学生管理、课次管理和路径/状态联动细节。
2. 补齐系统用户管理、设置页等剩余页面层缺口。
3. 继续压缩旧口径、旧字段和兼容逻辑。
4. 为复杂页面继续补轻量接口与异步加载配套。
5. 开始落 Redis 非会话能力。

## 6. 风险

- 不能把“主表结构已收口”误判为“页面层已经完成”，当前最大缺口仍是页面细节和交互一致性。
- 前后端仍有少量“结构已改新、细节口径未全删净”的过渡状态，容易在联调时反复冒出边角问题。
- 当前采取的是直接改造方式，不依赖完整迁移脚本，运行环境必须保证数据库结构与代码一致。
- 异步拆分如果只拆组件、不拆接口，会变成“包小了，但数据仍然阻塞首屏”的假优化。

## 7. 约束

- 不修改 `wack-backend/README.md`、`wack-frontend/README.md`、`README_old.md`。
- 兼容性代码允许阶段性存在，但最终必须删除。
- `wack/` 主文档中已移除的设计，最终都必须从代码中删除。
